Name:           ros-lunar-pyros-utils
Version:        0.1.4
Release:        0%{?dist}
Summary:        ROS pyros_utils package

Group:          Development/Libraries
License:        BSD
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-lunar-rosgraph >= 1.11.19
Requires:       ros-lunar-roslaunch >= 1.11.19
Requires:       ros-lunar-rospy >= 1.11.19
Requires:       ros-lunar-rostest >= 1.11.19
BuildRequires:  ros-lunar-catkin >= 0.6.18
BuildRequires:  ros-lunar-catkin-pip >= 0.2.0
BuildRequires:  ros-lunar-pyros-test >= 0.0.3
BuildRequires:  ros-lunar-rosgraph >= 1.11.19
BuildRequires:  ros-lunar-roslaunch >= 1.11.19
BuildRequires:  ros-lunar-roslint >= 0.10.0
BuildRequires:  ros-lunar-rosnode >= 1.11.19
BuildRequires:  ros-lunar-rospy >= 1.11.19
BuildRequires:  ros-lunar-rostest >= 1.11.19
BuildRequires:  ros-lunar-rosunit >= 1.11.12

%description
Miscellaneous tools for pyROS

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/lunar" \
        -DCMAKE_PREFIX_PATH="/opt/ros/lunar" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/lunar

%changelog
* Mon Aug 07 2017 AlexV <asmodehn@gmail.com> - 0.1.4-0
- Autogenerated by Bloom

